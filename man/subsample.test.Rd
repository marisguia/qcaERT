% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsample.test.R
\name{subsample.test}
\alias{subsample.test}
\alias{print.subsample_test}
\alias{as.data.frame.subsample_test}
\title{Subsample robustness test for QCA solutions}
\usage{
subsample.test(
  data,
  outcome,
  conditions = NULL,
  calib = c("fixed", "recompute"),
  raw.data = NULL,
  calib_spec = NULL,
  sample_n = NULL,
  sample_prop = NULL,
  reps = 100,
  stratify = c("none", "outcome", "user"),
  strata = NULL,
  seed = NULL,
  case_labels = NULL,
  incl.cut = 1,
  n.cut = 1,
  solution = "all",
  include = NULL,
  dir.exp = NULL,
  which_M = 1,
  i_mode = c("all", "C1P1"),
  exclude_mode = c("recompute", "static", "none"),
  exclude_recompute = list(type = 2),
  exclude_static = NULL,
  fit_measures = c("inclS", "PRI", "covS"),
  fit_tol = 0,
  progress = TRUE,
  ...
)

\method{print}{subsample_test}(x, row.names = FALSE, ...)

\method{as.data.frame}{subsample_test}(x, ...)
}
\arguments{
\item{data}{A data frame object containing the outcome and condition
columns used in the QCA analysis. \code{data} must have at least
three rows.}

\item{outcome}{Name of the outcome. This must be a single
non-empty character string.}

\item{conditions}{Optional character vector of condition names. If \code{NULL},
the condition set is left to \code{\link[QCA:truthTable]{QCA::truthTable()}}. When
\code{calib = "recompute"}, \code{conditions} must be supplied explicitly.}

\item{calib}{Calibration handling for subsample runs. \code{"fixed"} uses the
calibrated values already present in \code{data}. \code{"recompute"} recalibrates
the outcome and conditions within each subsample using \code{raw.data} and
\code{calib_spec}.}

\item{raw.data}{Raw-data frame object used when \code{calib = "recompute"}.
It must have the same number of rows as \code{data}.}

\item{calib_spec}{Calibration specification used when
\code{calib = "recompute"}. This must be a named list keyed by
\code{c(outcome, conditions)}. Each entry must describe the raw source column,
the set type, and the calibration inputs used to rebuild the calibrated
set. Use the same \code{calib_spec} structure described for \code{\link[=calib.test]{calib.test()}}. An
entry may additionally contain \code{findTh}, a named list of \code{\link[QCA:findTh]{QCA::findTh()}}
arguments; when supplied, thresholds are re-estimated within each
subsample instead of reusing the baseline \code{thresholds}.}

\item{sample_n}{Integer subsample size. Supply exactly one of \code{sample_n} or
\code{sample_prop}.}

\item{sample_prop}{Proportion used to determine the realized subsample size.
Supply exactly one of \code{sample_n} or \code{sample_prop}. The realized sample
size is \code{round(sample_prop * nrow(data))}.}

\item{reps}{Number of subsample replications.}

\item{stratify}{Stratification mode. \code{"none"} samples from the full dataset
without stratification, \code{"outcome"} stratifies on the baseline analysis
outcome, and \code{"user"} stratifies on \code{strata}.}

\item{strata}{Optional stratification column used when
\code{stratify = "user"}. It must have length \code{nrow(data)} and contain no
missing values.}

\item{seed}{Optional integer seed passed to \code{\link[=set.seed]{set.seed()}} before drawing the
subsamples.}

\item{case_labels}{Optional character vector of case labels with length
\code{nrow(data)}. If \code{NULL}, the function uses \code{rownames(data)} when
available; otherwise it uses row numbers converted to character.}

\item{incl.cut}{Inclusion cutoff passed to \code{\link[QCA:truthTable]{QCA::truthTable()}}. This must
be a single finite number in \verb{[0, 1]}.}

\item{n.cut}{Truth table frequency cutoff passed to \code{\link[QCA:truthTable]{QCA::truthTable()}}.
This must be an integer-like value of at least \code{1}.}

\item{solution}{Solution type to monitor. Accepted values are \code{"all"},
\code{"con"} or \code{"conservative"}, \code{"par"} or \code{"parsimonious"}, and \code{"int"} or
\code{"intermediate"}.}

\item{include}{Optional minimization include setting. Currently, this
argument accepts only \code{NULL}, \code{""}, or \code{"?"}.}

\item{dir.exp}{Directional expectations used when the monitored solution is
intermediate.}

\item{which_M}{Positive integer giving which solution alternative to use
when minimization returns multiple models.}

\item{i_mode}{Character string controlling intermediate-solution selection.
Accepted values are \code{"all"} and \code{"C1P1"}.}

\item{exclude_mode}{Character string controlling how excluded rows are
handled for parsimonious and intermediate minimization. \code{"recompute"}
recalculates exclusions from each subsample truth table, \code{"static"}
reuses \code{exclude_static}, and \code{"none"} does not use exclusions.}

\item{exclude_recompute}{Named list of arguments passed to \code{\link[QCA:findRows]{QCA::findRows()}}
when \code{exclude_mode = "recompute"}.}

\item{exclude_static}{Already computed exclusion object reused when
\code{exclude_mode = "static"}.}

\item{fit_measures}{Character vector of fit-measure names to compare between
the baseline run and each subsample run. Use \code{NULL} to disable fit
comparison.}

\item{fit_tol}{Non-negative tolerance used when deciding whether fit values
changed.}

\item{progress}{Logical; if \code{TRUE} and the session is interactive, show a
text progress bar.}

\item{...}{Additional arguments. In \code{\link[=subsample.test]{subsample.test()}}, named arguments
matching \code{\link[QCA:truthTable]{QCA::truthTable()}} formals are passed to \code{\link[QCA:truthTable]{QCA::truthTable()}},
and the remaining named arguments are forwarded to \code{\link[QCA:minimize]{QCA::minimize()}}
after removing names reserved by \code{subsample.test()}. The function
also looks in \code{...} for \code{include}, \code{dir.exp}, or \code{direxp} if those
arguments were not supplied explicitly. In \code{\link[=print.subsample_test]{print.subsample_test()}},
\code{...} is passed to \code{\link[=print.data.frame]{print.data.frame()}}. In
\code{\link[=as.data.frame.subsample_test]{as.data.frame.subsample_test()}}, \code{...} is ignored.}

\item{x}{A \code{subsample_test} object returned by \code{\link[=subsample.test]{subsample.test()}}.}

\item{row.names}{Logical; passed to \code{\link[=print.data.frame]{print.data.frame()}} by
\code{\link[=print.subsample_test]{print.subsample_test()}}.}
}
\value{
An object of class \code{subsample_test} with the following components:
\describe{
\item{\code{diagnostics}}{A detailed data frame with one row per subsample
run. It records the replication number, subsample size, holdout size,
run status, solution-change classification, fit-change classification,
exact-match status relative to the baseline solution, term-set Jaccard
similarity to the baseline solution, and error information when a run
fails.}
\item{\code{results}}{A compact data frame with the columns
\code{rep}, \code{n_sample}, \code{n_holdout}, \code{status}, \code{exact_match_baseline},
\code{term_jaccard_baseline}, \code{solution_change}, \code{fit_changed_types},
\code{n_fit_deltas}, and \code{max_abs_fit_delta}.}
\item{\code{summary}}{A list with summary objects named \code{exact_solution},
\code{term_stability}, \code{fit_stability}, \code{similarity}, and \code{calibration}.}
\item{\code{baseline}}{A list containing the baseline analysis built from the
full dataset, including the truth table, minimization output,
selected solution terms used for comparison, fit values, exclusion
information, and calibration information.}
\item{\code{by_run}}{A named list with one entry per subsample run. Each
entry stores the sampled and held-out cases, run status, baseline and
subsample analyses, change summaries, fit deltas, exclusion
information, calibration information, and any error information for
that run.}
\item{\code{settings}}{A list containing the analysis settings used to build
the result object.}
}

\code{print.subsample_test()} prints a concise summary and the \code{results}
table. \code{as.data.frame.subsample_test()} returns the \code{results} table.
}
\description{
Draws repeated subsamples without replacement, reruns the QCA analysis on
each subsample, and records whether the monitored solution changes, whether
selected fit measures change, and whether a subsample run ends with an
error. The function can sample without stratification, stratify by the
baseline outcome, or stratify by a grouping column. This is a stringent,
punishing robustness check and is most useful when sample-composition
sensitivity is substantively important.
}
\details{
The function first runs the baseline analysis on the full dataset. It then
draws \code{reps} subsamples without replacement and reruns the analysis on each
subsample.

Exactly one of \code{sample_n} or \code{sample_prop} must be supplied. The realized
subsample size must be at least \code{2} and strictly smaller than
\code{nrow(data)}.

When \code{stratify = "outcome"}, the function stratifies on the baseline
analysis outcome and therefore requires that outcome to be crisp/binary
\code{0/1}. When \code{stratify = "user"}, the function stratifies on the supplied
\code{strata} vector.

When \code{calib = "fixed"}, the subsample runs use the calibrated values
already present in \code{data}. When \code{calib = "recompute"}, the function rebuilds
the calibrated outcome and condition columns within each subsample using
\code{\link[QCA:findTh]{QCA::findTh()}} and \code{\link[QCA:calibrate]{QCA::calibrate()}} as specified in \code{calib_spec}.

The shared solution-control, exclusion, and returned-object conventions are
described in \code{?qcaERT_conventions}.
}
\examples{
\donttest{
library(QCA)
data(LR)

conditions <- c("DEV", "URB", "LIT", "IND", "STB")
outcome <- "SURV"
dir_exp <- rep("1", length(conditions))

thresholds <- list(
  DEV = findTh(LR$DEV, groups = 7),
  URB = findTh(LR$URB, groups = 4),
  LIT = findTh(LR$LIT, groups = 4),
  IND = findTh(LR$IND, groups = 4),
  STB = findTh(LR$STB, groups = 4),
  SURV = findTh(LR$SURV, groups = 4)
)

dat <- LR
dat$DEV <- calibrate(LR$DEV, type = "fuzzy", thresholds = thresholds$DEV)
dat$URB <- calibrate(LR$URB, type = "fuzzy", thresholds = thresholds$URB)
dat$LIT <- calibrate(LR$LIT, type = "fuzzy", thresholds = thresholds$LIT)
dat$IND <- calibrate(LR$IND, type = "fuzzy", thresholds = thresholds$IND)
dat$STB <- calibrate(LR$STB, type = "fuzzy", thresholds = thresholds$STB)
dat$SURV <- calibrate(LR$SURV, type = "fuzzy", thresholds = thresholds$SURV)

out <- subsample.test(
  data = dat,
  outcome = outcome,
  conditions = conditions,
  calib = "fixed",
  sample_prop = 0.8,
  reps = 50,
  seed = 123,
  case_labels = rownames(dat),
  incl.cut = 0.8,
  n.cut = 1,
  solution = "all",
  dir.exp = dir_exp,
  fit_measures = c("inclS", "PRI", "covS"),
  progress = TRUE
)

out
as.data.frame(out)
}

}
\seealso{
\code{\link[=calib.test]{calib.test()}}, \code{\link[=incl.test]{incl.test()}}, \code{\link[=ncut.test]{ncut.test()}}, \code{\link[=loo.test]{loo.test()}},
\code{\link[=altset.test]{altset.test()}}, \code{\link[=theory.test]{theory.test()}}, \code{\link[=cluster.test]{cluster.test()}}, \code{\link[=sol.df]{sol.df()}}
}
